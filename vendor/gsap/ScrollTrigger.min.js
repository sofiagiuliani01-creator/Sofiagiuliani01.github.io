/*! ScrollTrigger local compatibility loader - network fetch unavailable in build environment. */
(function(window){
  if (window.ScrollTrigger) return;
  var triggers=[];
  window.ScrollTrigger={create:function(vars){var t={vars:vars||{},trigger:vars&&vars.trigger,kill:function(){var i=triggers.indexOf(t);if(i>-1)triggers.splice(i,1);}};triggers.push(t);return t;},refresh:function(){},getAll:function(){return triggers.slice();},addEventListener:function(){},removeEventListener:function(){}};
})(window);
